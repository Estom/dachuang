decode  解码

encode  编码

str使用encode是错误的
Unicode 使用decode是错误的


open打开文件时，read读取的是str，需要进行decode
write写入是，如果参数是Unicode，需要进行encode

如果是其他编码形式的str，需要先decode，转成Unicode在encode

同一工程一定要使用相同的字符编码声明
抛弃str，全部使用Unicode


启发：面对出现过两次以上的问题，第二次解决时就要彻底解决，不然会还会再遇到